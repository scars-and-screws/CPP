# 📚 Modern C++ Notes & Mini Labs 4 M3 & U

> A curated, example-first notebook of core C++ features, intentionally written for fast revision and future GitHub sharing.

## 🧭 Table of Contents

- [Overview](#-overview)
- [Getting Started](#-getting-started)
- [Repository Map](#-repository-map)
- [Topic Guide](#-topic-guide)
  - [Fundamentals](#-fundamentals)
  - [Memory & Modularity](#-memory--modularity)
  - [Object-Oriented Core](#-object-oriented-core)
  - [STL & Advanced Utilities](#-stl--advanced-utilities)
- [Study Workflow](#-study-workflow)
- [Contributing & Next Steps](#-contributing--next-steps)

## ✨ Overview

- Self-contained `.cpp` files that double as concept notes and runnable demos.
- Heavy inline comments explain the _why_ behind each construct.
- Ideal for "open the file → scan the heading → run the snippet" style revision.

## 🚀 Getting Started

**Prerequisites**

- A C++17+ compiler (MSVC, Clang, or MinGW-w64 g++).
- PowerShell or your shell of choice for compiling each snippet.

**Compile any demo**

```powershell
# Build into the output/ folder (feel free to change the target)
g++ loops.cpp -std=c++17 -Wall -Wextra -O2 -o output/loops.exe

# Run the freshly built sample
./output/loops.exe
```

**Recommended layout**

- Keep binaries in `output/` (already tracked) to avoid cluttering the root.
- `cpp_shit.txt` and `data.bin` are generated by the File I/O demo—safe to delete between runs.

## 🗂️ Repository Map

```
CPP/
├── arays.cpp                  // Arrays & 2D matrices
├── classes.cpp                // Constructors, destructors, templates
├── cmath.cpp                  // <cmath> utilities
├── conditional_statements.cpp // if/else, switch, ternary
├── cpp_shit.txt               // Sample text output from File I/O
├── data_types.cpp             // Types, modifiers, auto, typedef
├── encapsulation.cpp          // BankAccount encapsulation case study
├── exception.cpp              // try/catch, std::exception
├── file_i_o.cpp               // Streams + binary read/write
├── functions.cpp              // Templates, overloads, lambdas
├── inheritance*.cpp           // Inheritance flavors & access control
├── input_output.cpp           // cout / cin / getline basics
├── loops.cpp                  // For/while/do-while, control flow
├── operator_*.cpp             // Expressions & operator overloading
├── pointers.cpp               // Pointer theory → dynamic memory
├── polymorphism.cpp           // Static vs dynamic polymorphism
├── references.cpp             // Reference semantics & pitfalls
├── stl.cpp                    // Containers, iterators, algorithms
├── string.cpp                 // std::string toolbox
├── virtual_func.cpp           // Virtual + pure-virtual demos
└── output/                    // Suggested binary drop zone
```

## 🧵 Topic Guide

### 🧱 Fundamentals

| File                           | What to revisit                                                          | Quick challenge                                                                                     |
| ------------------------------ | ------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------- |
| `input_output.cpp`             | `cout`, `cin`, buffered vs unbuffered streams.                           | Extend the sample to read a full line after integer input (handle the newline with `cin.ignore()`). |
| `data_types.cpp`               | Initialization styles, modifiers, `typedef`, `auto`, user-defined types. | Add another `enum class` and print its underlying type with `std::underlying_type`.                 |
| `operator_and_expressions.cpp` | Precedence, arithmetic/relational/logical/bitwise operators.             | Implement a short function that toggles the 3rd bit using the bitwise helpers shown.                |
| `conditional_statements.cpp`   | If ladders, nested `if`, ternary, `switch`.                              | Convert one `if-else` chain into a `switch` where possible.                                         |
| `loops.cpp`                    | For/while/do-while, nested loops, `break`/`continue`.                    | Rewrite the nested-loop block as a single loop over `std::pair<int,int>`.                           |
| `arays.cpp`                    | 1D/2D arrays, partial init, range-for traversals.                        | Replace the raw loops with a helper that prints any array via `std::size`.                          |
| `functions.cpp`                | Templates, references, default args, inline, recursion, lambdas.         | Add a constrained template (C++20 `concept`) around `maximum`.                                      |

### 🧠 Memory & Modularity

| File             | What to revisit                                                                   | Quick challenge                                                                           |
| ---------------- | --------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------- |
| `pointers.cpp`   | Declaration, dereferencing, pointer arithmetic, `new/delete`, dangling hazards.   | Extend the dynamic array example to use `std::unique_ptr<int[]>` as the safe alternative. |
| `references.cpp` | Binding rules, const refs, range-for references, pointer refs.                    | Demonstrate a function returning a reference to allow chaining assignments.               |
| `file_i_o.cpp`   | `ifstream/ofstream/fstream`, modes, newline handling, binary IO.                  | Update the sample to detect file size via `seekg`/`tellg` before reading.                 |
| `cmath.cpp`      | `sqrt`, `pow`, `abs`, `ceil/floor`, logs, rounding.                               | Add a short snippet that clamps a value using `std::fmin`/`std::fmax`.                    |
| `string.cpp`     | Constructors, `length`, `substr`, `find`, `copy`, `insert`, `erase`, conversions. | Implement a helper that title-cases a string using the existing casing loop.              |

### 🏛️ Object-Oriented Core

| File                             | What to revisit                                                                                         | Quick challenge                                                                              |
| -------------------------------- | ------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------- |
| `classes.cpp`                    | Access specifiers, constructors (default/param/copy), destructors, getters/setters, friends, templates. | Implement move semantics for `Car` or extend `Storage<T>` with `constexpr` helpers.          |
| `encapsulation.cpp`              | Data hiding via getters/setters (`BankAccount`).                                                        | Introduce input validation errors via `std::optional<std::string>` return codes.             |
| `inheritance.cpp`                | Single, multiple, multilevel, hierarchical, hybrid inheritance walkthroughs.                            | Add `override`d methods that call `Base::method()` to highlight reuse.                       |
| `inheritance_access_control.cpp` | Public vs protected vs private inheritance matrix.                                                      | Create a diagram (ASCII comment) summarizing access transitions per specifier.               |
| `polymorphism.cpp`               | Compile-time vs runtime polymorphism, virtual destructors.                                              | Add a base `Account` interface that exposes a pure virtual `audit()` for each derived class. |
| `virtual_func.cpp`               | Virtual dispatch, overriding, abstract classes with pure virtuals.                                      | Convert `Shape` into an interface that also enforces `area()`.                               |
| `operator_overloading.cpp`       | Binary/unary overloads, friend operators, rules & caveats.                                              | Overload `==` and `!=` for `Complex` to compare both parts.                                  |

### 🧰 STL & Advanced Utilities

| File            | What to revisit                                                       | Quick challenge                                                              |
| --------------- | --------------------------------------------------------------------- | ---------------------------------------------------------------------------- |
| `stl.cpp`       | Containers, iterators, `find`, `sort`.                                | Replace manual iterator loops with range-based loops plus `std::for_each`.   |
| `exception.cpp` | `try/catch`, custom exceptions, `std::runtime_error`, `what()`.       | Demonstrate stack unwinding by rethrowing exceptions from a helper function. |
| `file_i_o.cpp`  | (see above) also covers binary serialization with `reinterpret_cast`. | Serialize a small struct and read it back for verification.                  |
| `functions.cpp` | (see above) includes lambda captures—useful for algorithm callbacks.  | Feed `addLambda` into `std::accumulate` for a quick demo.                    |
| `cmath.cpp`     | (see above) pair well with numeric STL algorithms.                    | Integrate with `std::transform` to map `sqrt` over a vector.                 |

> 💡 **Tip:** Each file has a `main()` ready to run. Comment/uncomment blocks as you revise a chapter to keep stdout tidy.

## 🧑‍💻 Study Workflow

1. **Pick a topic** via the tables above (or `Ctrl+P` → file name).
2. **Skim comments first** to refresh the theory, then run the sample.
3. **Tweak one thing** (change input, add a `cout`, refactor to STL) and re-run.
4. **Log insights** in your commit messages so GitHub doubles as a revision log.
